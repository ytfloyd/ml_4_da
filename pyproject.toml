[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ml4t_crypto"
version = "0.1.0"
description = "MLAT-2e walkthrough on Coinbase crypto OHLCV"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
